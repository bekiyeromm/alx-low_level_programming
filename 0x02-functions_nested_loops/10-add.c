#include<stdio.h>
#include "main.h"
/**
 * add -calculate sum
 * @num1: num1 to be added
 * @num2: num2 to be added
 * Return: always 0
 */
int add(int num1, int num2)
{
	int sum;

	sum = num1 + num2;
	return (sum);
}
