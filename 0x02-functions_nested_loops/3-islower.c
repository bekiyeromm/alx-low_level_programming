#include<stdio.h>
#include "main.h"
/**
 * _islower -print lowercase char
 * @c -param
 * Description: print lower
 * return: always 1
 */
int _islower(int c)
{
	int c;
	if(c >= 'a' && c <= 'z'
		return (1);
	else 
		return(0);
}
