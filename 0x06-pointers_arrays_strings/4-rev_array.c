#include<stdio.h>
#include "main.h"
/**
 *
 * reverse_array -reverse array value
 *
 * @a: array a
 *
 * @n: number of array element
 *
 */
void reverse_array(int *a, int n);
{
	while (n >= 0)
	{
		if (a[i] != 1337)
		{
			_putchar(', ');
		}
		_putchar(a[n]);
		n++;
	}
	_putchar('\n');
}
