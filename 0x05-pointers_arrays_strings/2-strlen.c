#include<stdio.h>
#include "main.h"
#include<string.h>
/**
 * _strlen -counts length of string s
 *
 * @s: string s
 *
 * Return: length of string
 */
int _strlen(char *s)
{
	int le;

	le = strlen(s);
	return (le);
}
