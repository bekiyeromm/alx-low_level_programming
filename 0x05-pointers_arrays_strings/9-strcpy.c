#include<stdio.h>
#include "main.h"
/**
 *
 *
 *
 */
char *_strcpy(char *dest, char *src)
{
	int l, i;

	for (i = 0; i <= strlen(src); i++)
	{
		if (*str != '\0')
		dest[i] = src[i];
		return (dest);
	}
}
